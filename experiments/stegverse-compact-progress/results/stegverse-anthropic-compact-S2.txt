S2: Theorem formulation

---

## 1. Reused Admitted Artifacts

**From S1 (sha256:ba7aff1e81a6b2118b4a12c01ea4f78b78b3c369b4b06a12fc9a694158017340):**

- `C : Type` — abstract candidate type
- `GBP BD OC : C → Prop` — total predicates, no assumed relationships, no decidability
- **DEF-ALLOW (S-DEF):** `ALLOW(c) := GBP(c) ∧ BD(c) ∧ OC(c)`

All items remain stipulations. No deployed system conformance is claimed.

---

## 2. Theorem Statement

**THM-ALLOW-IFF (S2-MAIN).**

> For all `c : C`,
> `ALLOW(c) ↔ GBP(c) ∧ BD(c) ∧ OC(c)`

This is the necessary-and-sufficient characterization of `ALLOW` admissibility. It follows directly from DEF-ALLOW (S-DEF) by definitional unfolding; no additional premises are required.

**Status:** *Stipulated theorem* — provable by reflexivity of `↔` under the given definition. Not an empirical claim about any implementation.

---

## 3. Decomposed Directions

**THM-ALLOW-FWD (S2-FWD).**
> `ALLOW(c) → GBP(c) ∧ BD(c) ∧ OC(c)`

*(Necessity: if the evaluator returns ALLOW, then all three predicates hold.)*

**THM-ALLOW-BWD (S2-BWD).**
> `GBP(c) ∧ BD(c) ∧ OC(c) → ALLOW(c)`

*(Sufficiency: if all three predicates hold, the evaluator returns ALLOW.)*

Both directions follow immediately from DEF-ALLOW (S-DEF) by unfolding.

---

## 4. Claim Boundary

| Item | Type | Status |
|---|---|---|
| THM-ALLOW-IFF | Necessary-and-sufficient characterization | Stipulated; follows by definition |
| THM-ALLOW-FWD | Necessity direction | Stipulated; follows by definition |
| THM-ALLOW-BWD | Sufficiency direction | Stipulated; follows by definition |
| Implementation binding | Any deployed GCAT/BCAT system conforms | **Unresolved; not claimed** |
| Decidability of predicates | GBP, BD, OC are decidable | **Not assumed; not claimed** |
| Independence of predicates | GBP, BD, OC are mutually independent | **Not assumed; not claimed** |

---

## 5. Lean 4 Candidate — Logical Core

```lean4
-- S2: Lean 4 candidate for logical core
-- STIPULATION: Abstract model only. Does not assert conformance
-- of any deployed GCAT/BCAT implementation.
-- Implementation validity is NOT claimed.

variable {C : Type}
variable (GBP BD OC : C → Prop)

/-- DEF-ALLOW (S-DEF): definitional equality, carried forward from S1. -/
def ALLOW (c : C) : Prop := GBP c ∧ BD c ∧ OC c

/-- THM-ALLOW-IFF (S2-MAIN):
    Necessary-and-sufficient characterization of ALLOW admissibility.
    Proof: by definitional unfolding (Iff.rfl). -/
theorem th