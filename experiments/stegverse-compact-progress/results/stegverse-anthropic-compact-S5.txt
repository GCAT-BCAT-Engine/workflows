S5: Lean 4 candidate.

---

## Stage S5 — Lean 4 Candidate

**Status:** Candidate only. Not claimed to compile in any specific Lean 4 toolchain version. Not claimed to reflect any deployed GCAT/BCAT implementation. Artifact identity is established by the hash below; semantic validity is unverified.

---

### Governance Notes

- All items are **stipulations** for this experiment (EXPERIMENT: SV-COST-STAGED-001).
- `DEF-ALLOW` is carried forward from S1 (sha256:ba7aff1e81a6b2118b4a12c01ea4f78b78b3c369b4b06a12fc9a694158017340).
- `THM-ALLOW-IFF` is carried forward from S2 (sha256:7f8dec22c9051057f9d28390c606bb328f525cd9ed6ce775bf0f3bc4d26069e8).
- Proof strategy (definitional unfolding, `intro`/`exact`, `Iff.intro`) is carried forward from S3 and S4.
- No claim is made that this candidate has been type-checked or that any deployed system conforms.

---

### Lean 4 Candidate Source

```lean4
/-
  EXPERIMENT: SV-COST-STAGED-001
  CAPABILITY TEST: SV-COMPACT-PROGRESS-001
  STAGE: S5 — Lean 4 candidate

  STATUS: Candidate only.
  - Not claimed to compile in any specific Lean 4 toolchain version.
  - Not claimed to reflect any deployed GCAT/BCAT implementation.
  - All definitions and theorems are stipulations for this experiment.
  - No claim of implementation conformance is made anywhere in this file.
-/

-- ============================================================
-- §1. Universe of candidates
-- ============================================================

/-- `C` is an arbitrary type representing the universe of candidate
    transitions submitted to the abstract GCAT/BCAT evaluator.
    No structure is assumed beyond membership in `Type`. -/
variable (C : Type)

-- ============================================================
-- §2. Primitive predicates (DEF-PRIM, S1)
--
-- `GBP`, `BD`, and `OC` are stipulated as total predicates on `C`.
-- No relationship among them is assumed.
-- No decidability assumption is made.
-- ============================================================

variable (GBP : C → Prop)
variable (BD  : C → Prop)
variable (OC  : C → Prop)

-- ============================================================
-- §3. The ALLOW predicate (DEF-ALLOW, S1 / S-DEF)
--
-- ALLOW(c) := GBP(c) ∧ BD(c) ∧ OC(c)
--
-- This is a definitional identity, not an empirical claim.
-- ============================================================

/-- `ALLOW c` is defined as the conjunction of `GBP c`, `BD c`, and `OC c`.
    This is DEF-ALLOW (S-DEF) from Stage S1. -/
def ALLOW (c : C) : Prop :=
  GBP c ∧ BD c ∧ OC c

-- ============================================================
-- §4. Sub-lemmas (LEM-FWD and LEM-BWD, S3)
-- ============================================================

/-- LEM-FWD (Necessity):
    If `ALLOW c` holds, then `GBP c ∧ BD c ∧ OC c` holds.
    Proof: definitional unfolding; the hypothesis is the conclusion. -/
theorem lem_f