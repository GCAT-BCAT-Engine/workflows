S0: Task parsing and claim boundary.

**Task identity:** SV-COST-STAGED-001 / SV-COMPACT-PROGRESS-001

**Stipulations**
- For a candidate transition `t`, `GBP t`, `BD t`, and `OC t` are predicates.
- The abstract evaluator returns `ALLOW` exactly when all three predicates hold.
- No artifacts are admitted from prior stages.

**Target claim**
- Necessary and sufficient condition:
  `ALLOW(t) ↔ (GBP(t) ∧ BD(t) ∧ OC(t))`.
- Equivalently, necessity is `ALLOW(t) → GBP(t) ∧ BD(t) ∧ OC(t)`, and sufficiency is the converse.

**Claim boundary**
- The target concerns only the stipulated abstract evaluator.
- It does not establish that any deployed or concrete GCAT/BCAT implementation uses these predicates, computes them correctly, or conforms to this characterization.
- No implementation validity, refinement, or correspondence claim is in scope.
- A Lean 4 candidate for the logical core belongs to a later stage and is not supplied here.

**Verification status**
- Generated target claim: identified but not yet formally derived.
- Verified claims: none.
- Unresolved evidence: all implementation-level definitions, behavior, and conformance.