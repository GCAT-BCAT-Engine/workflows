S0: Task parsing and claim boundary.

**Task identity:** `SV-COST-STAGED-001` / `SV-COMPACT-PROGRESS-001`

**Stipulation**
- For any candidate transition `t`, the abstract evaluator returns `ALLOW` exactly when `GBP(t)`, `BD(t)`, and `OC(t)` all hold.

**Target claim**
\[
\operatorname{ALLOW}(t)\iff GBP(t)\land BD(t)\land OC(t).
\]

Equivalently:
- **Necessity:** `ALLOW(t) → GBP(t) ∧ BD(t) ∧ OC(t)`.
- **Sufficiency:** `GBP(t) ∧ BD(t) ∧ OC(t) → ALLOW(t)`.

**Claim boundary**
- The characterization concerns only the stipulated abstract evaluator and its three predicates.
- No meaning or properties of `GBP`, `BD`, or `OC` beyond propositional predicates are assumed.
- No claim is made that any deployed or concrete GCAT/BCAT implementation satisfies this specification.
- No claim is made about transition validity, runtime behavior, completeness, soundness, or implementation correspondence.

**Verified claims**
- None at stage S0.

**Unresolved implementation evidence**
- No implementation, predicate definitions, evaluator code, or conformance evidence has been provided.
- A Lean 4 candidate and logical verification remain for later stages.