S0: Task parsing and claim boundary.

## Stipulations

- Let `τ` range over candidate transitions.
- `GBP τ`, `BD τ`, and `OC τ` are predicates.
- Let `ALLOW τ` mean that the abstract evaluator returns `ALLOW` for `τ`.
- The evaluator specification stipulates that it returns `ALLOW` exactly when all three predicates hold.

## Target generated claim

The logical characterization to be established in a later stage is:

\[
\forall \tau,\quad ALLOW(\tau) \leftrightarrow
\bigl(GBP(\tau)\land BD(\tau)\land OC(\tau)\bigr).
\]

This comprises:

- **Necessity:** `ALLOW τ → GBP τ ∧ BD τ ∧ OC τ`.
- **Sufficiency:** `GBP τ ∧ BD τ ∧ OC τ → ALLOW τ`.

A later Lean 4 candidate should encode only this abstract propositional core, parameterized by a transition type and the four predicates.

## Claim boundary

The intended claim concerns only the stipulated abstract evaluator relation. It does not establish:

- conformity of any deployed or concrete GCAT/BCAT implementation;
- correctness of implementations of `GBP`, `BD`, or `OC`;
- any operational, security, performance, decidability, or termination property;
- correspondence between the abstract predicates and external system behavior;
- successful Lean parsing, elaboration, compilation, or proof checking.

## Verification status

- No prior artifacts are admitted.
- No proof has been supplied or checked at this stage.
- No Lean 4 candidate has yet been produced or type-checked.
- Implementation validity and implementation-to-specification correspondence remain unresolved.