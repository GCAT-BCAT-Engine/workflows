S1: Definitions and assumptions.

**Task identity:** `SV-COST-STAGED-001`  
**Capability test:** `SV-COMPACT-PROGRESS-001`

### Admitted dependency
- `S0` (`sha256:cf823f714fc36351325450d467e369aaac1fa755e8aac528aa358717cb001e5d`) supplies the task boundary and abstract `ALLOW` stipulation.
- The hash identifies the admitted artifact; it is not treated as evidence of semantic or implementation correctness.

### Definitions
Let:
- `Transition` be an arbitrary type of candidate transitions.
- `GBP BD OC : Transition → Prop` be predicates on candidate transitions.
- `Outcome` contain at least the result `ALLOW`.
- `eval : Transition → Outcome` be the abstract evaluator.
- `Allowed (t : Transition) : Prop` abbreviate `eval t = ALLOW`.

For each `t : Transition`, define the combined condition:
\[
Conditions(t) \;:=\; GBP(t)\land BD(t)\land OC(t),
\]
with conjunction parsed as `GBP t ∧ (BD t ∧ OC t)`.

### Stipulated assumption
The abstract evaluator is governed by:
\[
\forall t:\mathrm{Transition},\quad
Allowed(t)\leftrightarrow Conditions(t).
\]

Equivalently:
\[
\forall t,\quad
eval(t)=ALLOW\leftrightarrow
\bigl(GBP(t)\land BD(t)\land OC(t)\bigr).
\]

“Exactly when” supplies both directions:
- `Allowed t → GBP t ∧ BD t ∧ OC t`;
- `GBP t ∧ BD t ∧ OC t → Allowed t`.

### Generated claims
- The later necessary-and-sufficient characterization should quantify over every candidate transition and use the stipulated biconditional above.
- No proof or Lean 4 candidate is supplied at this stage.

### Verified claims
- None. The definitions and evaluator condition are stipulations, not independently verified facts.

### Claim boundary
- These definitions concern only the stipulated abstract evaluator.
- They do not establish that any deployed, source-level, compiled, or runtime GCAT/BCAT implementation uses these predicates or satisfies this condition.
- No Lean elaboration, compilation, runtime behavior, or implementation correspondence has been verified.

### Unresolved implementation evidence
- Logical derivation of the characterization.
- Lean 4 encoding and elaboration/compilation evidence.
- Correspondence between the abstract model and any concrete GCAT/BCAT implementation.