S2: Theorem formulation.

**Task identity:** `SV-COST-STAGED-001`  
**Capability test:** `SV-COMPACT-PROGRESS-001`

### Admitted dependencies
- `S0` (`sha256:cf823f714fc36351325450d467e369aaac1fa755e8aac528aa358717cb001e5d`) supplies the task boundary.
- `S1` (`sha256:7f0e127e6f2a8a1fbd9f8af01211a5dfbacfcc5cafddf77dd988c85c68a08165`) supplies the abstract definitions and evaluator stipulation.
- These hashes identify admitted artifacts; they are not evidence of semantic correctness or implementation conformity.

### Stipulations reused
Let:
- `Transition` be an arbitrary type;
- `GBP BD OC : Transition → Prop`;
- `Outcome` contain `ALLOW`;
- `eval : Transition → Outcome`;
- `Allowed t := eval t = ALLOW`;
- `Conditions t := GBP t ∧ (BD t ∧ OC t)`.

Assume:
\[
\forall t:\mathrm{Transition},\quad
Allowed(t)\leftrightarrow Conditions(t).
\]

### Theorem formulation
For every candidate transition `t`,
\[
eval(t)=ALLOW
\quad\leftrightarrow\quad
GBP(t)\land\bigl(BD(t)\land OC(t)\bigr).
\]

Thus, relative to the stipulated abstract evaluator:

- **Necessity:** If `eval t = ALLOW`, then `GBP t`, `BD t`, and `OC t` all hold.
- **Sufficiency:** If `GBP t`, `BD t`, and `OC t` all hold, then `eval t = ALLOW`.

### Lean 4 candidate theorem statement
```lean
theorem allow_iff_conditions
    {Transition Outcome : Type}
    (ALLOW : Outcome)
    (eval : Transition → Outcome)
    (GBP BD OC : Transition → Prop)
    (allow_spec :
      ∀ t, eval t = ALLOW ↔ GBP t ∧ (BD t ∧ OC t)) :
    ∀ t, eval t = ALLOW ↔ GBP t ∧ (BD t ∧ OC t) :=
  by
    intro t
    exact allow_spec t
```

This is a candidate statement of the logical core; Lean elaboration or compilation is not asserted.

### Generated claims
- The displayed biconditional is the necessary-and-sufficient characterization of `ALLOW` under the admitted stipulation.
- Its necessity and sufficiency clauses are the two directions of that biconditional.
- The Lean candidate directly exposes the stipulated specification as its hypothesis and conclusion.

### Verified claims
- None independently verified. At this stage, the theorem formulation follows from admitted stipulations; no formal compilation or external semantic validation has been performed.

### Claim boundary
- The theorem concerns only the stipulated abstract evaluator.
- It does not show that any deployed, source-level, compiled, or runtime GCAT/BCAT evaluator uses these predicates or conforms to this specification.
- The Lean text is only a candidate for the logical core and does not establish implementation validity.

### Unresolved implementation evidence
- Lean 4 elaboration and compilation in a specified toolchain and environment.
- Formal proof checking beyond inspection of the candidate.
- Any correspondence between the abstract model and a concrete GCAT/BCAT implementation.