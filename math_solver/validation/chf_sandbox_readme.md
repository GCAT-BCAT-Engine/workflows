# CHF Sandbox Runner — v0.13

v0.13 keeps the existing generated sandbox suites and adds explicit deterministic specs for `chf-031` through `chf-040`.

Generated suites for these new ecosystem gates can be added in a later batch after the explicit semantics are confirmed.
